# Project Objective: simple-search v1.2 — data enrichment + improvement roadmap (csl-apidev#26)

Document the v1.1 "simple search" engine (`simple-search/`) and hand Jim a
ready-to-implement v1.2 roadmap that (a) cuts overgeneration and (b) widens
the ways a user may spell Sanskrit. Planning/docs only this session — Jim
implements the PHP later from the roadmap. Feeds the v1.1 rollout/feedback
issue [csl-apidev#26](https://github.com/sanskrit-lexicon/csl-apidev/issues/26).

## ➡️ Next Steps (Queue)
- **@WAITING (`app/` slice 1 — live verification only):** slice 1 SHIPPED
  04-07-2026 (see Completed) — all 8 acceptance criteria pass offline via
  `app/index.php?fixtures=1`. Remaining, gated on the Cologne outage
  ([SERVER_OUTAGES.md](https://github.com/gasyoun/Uprava/blob/main/SERVER_OUTAGES.md)):
  (1) live re-verification with ≥10s-spaced probes (AC7's live half), including
  the new Velthuis input path,
  (2) recapture the synthetic dalglob/batch fixtures from the live endpoints
  (see [app/fixtures/readme.md](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/app/fixtures/readme.md)),
  (3) Jim's server-checkout pull (code inert on the live site until then, same
  as lookup Wave 1 [PR #63](https://github.com/sanskrit-lexicon/csl-apidev/pull/63)).
  Slice 2 (catalogue homepage, dictionary detail routes, suffix-mode wiring
  after identifying the Advanced page's endpoint) is a separate future handoff.
  **04-07-2026 addendum (MG ruling, R8):** input-scheme select finalized —
  Default, IAST, HK, Devanagari, SLP1, Velthuis, ITRANS (WX excluded,
  storage-only). Velthuis built end-to-end this session: new
  [velthuis_slp1.xml](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/utilities/transcoder/velthuis_slp1.xml)
  transcoder table + `input_simple == 'velthuis'` branch in
  [simple_search.php](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/simple-search/v1.1/simple_search.php)
  (MG-authorized narrow exception to the v1.1-frozen convention, mirrors the
  hk/itrans branches exactly — v1.1a NOT touched). Verified: `node --check`
  clean on `app.js`, XML well-formed; **not** verified live (no PHP runtime in
  this environment and the server is down) — flag `input=velthuis` for the
  live re-verification pass above. Roadmap Q5 closed in
  [roadmap_v1.2.md](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/simple-search/roadmap_v1.2.md).
- **@WAITING (H122 step 8 — live rerun only):** [H122](https://github.com/gasyoun/Uprava/blob/main/handoffs/H122-Sonnet_csl-apidev_simple_search_review_fixups_03.07.26.md)
  steps 1-7 (all doc/gate fix-ups: C1/M1/M2/M3/MINORs/NITs) are DONE —
  [PR #66](https://github.com/sanskrit-lexicon/csl-apidev/pull/66) merged
  03-07-2026. Only the live re-run remains, and it's still blocked: a fresh
  liveness probe today (`curl -sI --max-time 15 .../getword_list_1.0.php?...&key=agni`)
  timed out (exit 28) — `www.sanskrit-lexicon.uni-koeln.de` is still on the
  [SERVER_OUTAGES.md](https://github.com/gasyoun/Uprava/blob/main/SERVER_OUTAGES.md)
  board. When it recovers: liveness-check first, then
  `python simple-search/eval/eval_search.py --live` GENTLY (≥10s between
  requests — host rate-limits bursts), capture fixtures for the new
  `mani`/`guna`/`hari`/`giri` C1/M3 tripwire rows in `gold.tsv`, run the
  addendum probes (`vftti`, `praC`, `ca`), and flip that outage row to
  ✅ Recovered.
- **@WAITING (CI):** [PR #67](https://github.com/sanskrit-lexicon/csl-apidev/pull/67)
  (`lookup/` Wave 2) auto-merge armed, awaiting checks (see Completed below
  for what shipped).
- **`lookup/` Wave 3** — unblocked by Wave 2 merge + live-server verification of
  Wave 1 latency (roadmap §Wave 3): per-entry scan click-through via
  `servepdf.php`, copy-citation button (#29 formats), accent toggle + print
  stylesheet + dark mode, deprecation-ask PR for `dalglob1.php`.
- **@WAITING (Jim):** server-checkout pull for [PR #63](https://github.com/sanskrit-lexicon/csl-apidev/pull/63)
  (`lookup/` Wave 1) — the new `lookup/index.php`/`getword_batch.php` are inert
  on the live site until the server's git checkout is updated; client falls
  back to sequential `getword.php` fetches in the meantime.
- Post the drafted v1.2 feedback comment to csl-apidev#26 (links the roadmap +
  the default-mode-only finding). Blocked from this host by the api.github.com
  filter; post from the web UI or an API-reachable network.
- **Wire it in (Jim):** point `init_word_frequency()` (v1.1/getword_list_1.0_main.php)
  at `simple-search/wf1/wf.txt` instead of `wf0/wf.txt`. The data is built; v1.1
  code is frozen so the one-line switch is Jim's. (Fix I, roadmap §12.)
- Jim: implement v1.2 from `simple-search/roadmap_v1.2.md`, milestones M1→M5
  (M1 = precise-table + dedup + NFC; biggest win, no new data).
- Resolve the 10 open Questions in §14 of the roadmap (hard-drop DELTA, x→kṣ vs
  x→z conflict, sh→ś bias, add `score` to JSON contract, WX/Velthuis as explicit
  modes, Brahmic detect order, PHP intl/Normalizer on server, phonotactic
  exceptions, index-side rewrite worth it?, case-significant ASCII policy).
- Optional: link `roadmap_v1.2.md` from issue #26 as the feedback response.
- (Parallel thread) COLOGNE#249 clean-URL permalinks: roadmap `doc/cleanurl.md`,
  now **unified with the Salt API permalink** (see Completed). Awaits Jim's shared,
  content-negotiated `/{DICT}/{ref}` rewrite + `cleanurl.php`. Keep #249 open.
  Open for Jim: Salt `salt_entries.md` §1.7 rewrite must adopt cleanurl's dict-code
  whitelist + homonym/decimal-lnum handling; decide content-negotiation trigger
  (`Accept` vs `.json`/`?format=` override — cleanurl Q8).

## 🚧 Current Work-In-Progress (WIP)
- None open for this session (lookup Wave 2 shipped + merged, see Completed).
  The only loose end from earlier is the #26 feedback comment: it is drafted
  but CANNOT be posted from this host — `api.github.com` is blocked (gh
  GraphQL/REST = TLS handshake timeout), while `github.com` git push works
  fine. Post via web UI or an API-reachable network.
- **Not mine — leave alone:** `basicadjust.php` has an uncommitted local
  modification (H139 Vārttika→parent-sūtra `vartt_href_callback_mw` work,
  dated 03-07-2026) from a concurrent session sharing this working tree. Did
  not touch it this session.

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)
- **Engine map:** `Simple_Search` ctor (`simple-search/v1.1/simple_search.php`)
  = convert_nonascii → dalnorm.normalize → doVariant recursion over
  `transitionTable_default` (broad) pruned by `ngramValidate` (LIKE 'pref%') →
  generate_alternate_endings + grammar_variants → kept iff normalizes to a
  hwnorm1c headword → order_by_wf. JSON built in `getword_list_1.0_main.php`.
- **Overgeneration root cause:** equivalence classes (nasals 6-way, sibilants,
  r-cluster 9-way, b/v) applied unconditionally + bidirectionally at every
  position; validation is existence-only (no distance/score); endings+grammar
  multiply again. Live MW counts (input=default, 2026-06-11): agni=1, deva=5,
  kara=11, sana=18, **manas=20**, xqzqxq=0. These are real, pulled from the
  live `getword_list_1.0.php`.
- **KEY CORRECTION (verified live):** overgeneration is **`default`-mode ONLY**.
  `restrict_to_user_word` (2021) returns just the exact word for any non-default
  input mode when that word exists — `input=iast` manas/sana/kara/saMskfta each
  return **1** (vs 20/18/11/— under default). So Fix A (precise table) is a
  *minor* lever; the headline fix is the default-mode score + hard-drop (Fix B /
  milestone M2). Both docs were corrected to say this after an initial overstatement.
- **Input coverage:** `convert_nonascii` auto-detects only Devanagari + Cyrillic.
  `utilities/transcoder/wx_slp1.xml` EXISTS but is unwired. Velthuis + all
  non-Devanagari Brahmic tables are absent. No NFC pass. Capital letters: default
  path `mb_strtolower`s everything (good for romanization, lossy for HK/SLP1/
  upper-ITRANS typed in default mode; combining marks on capitals need NFC).
- **Decisions locked (2026-06-11 feedback round):** trust precise input (no
  nasal/sibilant fuzz for slp1/deva/iast); result policy = score + HARD-DROP
  (not collapse, not reorder-only); add all four input tracks (loose-ASCII+case,
  Brahmic, WX+Velthuis, ISO-15919/NFC/case). Roadmap = `simple-search/roadmap_v1.2.md`.
- ASCII schemes (WX/Velthuis/HK) can't be auto-detected (mutually ambiguous) →
  explicit `<select>` options only; only NON-ASCII scripts go in the detect loop.
- **DH program (roadmap_dh.md).** Decisions locked: TEI Lex-0, Vidyut, full eval
  harness, all four streams. Frame = simple-search is the discovery/retrieval
  layer OVER the `csl-standards` stack, NOT a new data model. csl-standards already
  owns the neutral JSON model (stable ids e.g. `mw-pwg-pwk:aMSa`) + TEI-archival +
  OntoLex/FrAC + loss reports (its docs/INTEROPERABILITY_MODEL.md, PROJECT_SPEC.md).
  Streams: A lemmatization/sandhi (Vidyut, `analyze=yes`); B corpus-grounding (DCS
  examples/freq/genre, `corpus=yes`); C FAIR/TEI-Lex-0/LOD (reuse csl-standards id +
  content negotiation + #249 permalinks); D eval harness (built).
- Python can't reach the live Cologne API from this host (urllib fails like
  api.github.com); WebFetch works. So the eval harness runs on fixtures here, live
  on the server.
- **DCS-2026 frequency source (Fix I):** `VisualDCS/src/DCS-data-2026/exports/clean/
  lemmas.csv` = 15,902 IAST lemmas + `token_count`. Maps onto the SLP1 wf key space
  (`wf0/wf.txt`, 50,474 keys) via roman→slp1 + dalnorm. Coverage probe (approx, lower
  bound): 79.4% of lemmas hit an existing key, 93.6% of token mass; 3,270 misses are
  mostly DCS causative/sandhi stems, not gaps. token_count sums to ~134k = a filtered
  slice, NOT the 4.57M full corpus (`dcs_full.sqlite`); relative order is sound.
  Probe script: `%TEMP%\dcs_wf_probe.py`.
- **Concurrent editor:** a human collaborator commits+pushes my working tree under
  "human push" commits (e.g. f70668a) and edits `.ai_state.md` concurrently. Not
  destructive — but commit deliverables promptly and treat `.ai_state.md` as
  append-only. github push/fetch is intermittently TLS-flaky.
- **H083 do-not-file suppression verdict (2026-07-03):** production API
  (`getword.php`/`getsuggest.php`/`dal.php`) is exact/prefix-match only — NOT
  APPLICABLE, nothing there could fold a deliberately non-standard headword
  into a different one. `simple-search/v1.1/` DOES have real spelling-variant
  folding (`Dalnorm::normalize` + `doVariant`/`transitionTable_default`), but
  it is an unshipped prototype (v1.2 roadmap above), so no suppression check
  was wired this pass. Full writeup + the pre-ship gate for v1.2:
  [`doc/do_not_file_suppression_verdict.md`](doc/do_not_file_suppression_verdict.md).
  csl-atlas carries the live suppression check
  ([csl-atlas#188](https://github.com/sanskrit-lexicon/csl-atlas/pull/188)).

## ✅ Completed (Recent only) — 2026-07-06
- **H227 SHIPPED — crawlable SSR entry permalinks + structured data** (Fable 5
  `claude-fable-5`, handoff
  [H227](https://github.com/gasyoun/Uprava/blob/main/handoffs/H227-Opus_csl-apidev_ux_seo_crawlable_structured_data_06.07.26.md)):
  [app/entry.php](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/app/entry.php)
  server-renders the full stacked multi-dictionary entry (Dalglob +
  GetwordClass driven in-process with `dispopt=3`, the getword_batch.php
  pattern) — a bot with JS off gets the complete entry; unique
  title/description/canonical/OG per headword; JSON-LD `@graph`
  (Organization/WebSite `@id` spine, one `DefinedTerm` per page,
  `DefinedTermSet` per dictionary, BreadcrumbList) per the Yandex-first
  [SEO playbook](https://github.com/gasyoun/Uprava/blob/main/SEO_STRUCTURED_DATA_PLAYBOOK.md);
  DCS corpus band from wf1/wf.txt + versioned kosha-shape Cite block (L4);
  [app/dictmeta.php](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/app/dictmeta.php)
  (derived from lookup/dictmeta.js),
  [entry.css](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/app/entry.css)/
  [entry.js](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/app/entry.js)
  (progressive enhancement only),
  [app/.htaccess](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/app/.htaccess)
  clean URLs, [app/robots.txt](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/app/robots.txt),
  [scripts/build_sitemap.py](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/scripts/build_sitemap.py)
  (verified: 9 shards / 406,595 URLs from the real hwnorm1c.sqlite, 57 junk
  keys gated), plan + L1–L4 built-vs-spec'd status in
  [doc/ux-redesign/SEO_PLAN.md](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/doc/ux-redesign/SEO_PLAN.md).
  Verified offline (PHP 8.2.12 `php -S`, `?fixtures=1`): raw-curl full SSR,
  JSON-LD parses with zero dangling `@id` refs, homonym MW¹/MW² anchors,
  404+noindex miss, noindex single-dict/fixtures views, XSS probes clean,
  browser console clean. Live re-verification + sitemap deploy gated on the
  Cologne outage + Jim's checkout pull (SEO_PLAN §8 checklist).
- **H225 SHIPPED — four end-to-end design-direction prototypes** (Fable 5
  `claude-fable-5`, four Fable-inherited builder subagents + main-session
  gallery/verification; handoff
  [H225](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H225-Fable_csl-apidev_ux_4_prototypes_end_to_end_06.07.26.md)):
  [doc/ux-redesign/prototypes/](https://github.com/sanskrit-lexicon/csl-apidev/tree/main/doc/ux-redesign/prototypes)
  — 16 self-contained static screens (4 directions × 4 pages) + index gallery,
  merged via [PR #76](https://github.com/sanskrit-lexicon/csl-apidev/pull/76),
  live at
  [prototypes/index.html](https://sanskrit-lexicon.github.io/csl-apidev/doc/ux-redesign/prototypes/index.html).
  D1 Research Workbench (= ruled Proposal A), D2 Library Catalogue, D3 Minimal
  Student, D4 Data-dense Scholar; real public-domain entry text (MW 1899
  agní/dhárma/mánas/índra + Apte 1890 agniḥ) extracted read-only from csl-orig,
  canonical copy in
  [_data/entries.js](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/doc/ux-redesign/prototypes/_data/entries.js).
  Verified locally: 0 console errors, no 375px horizontal scroll, nav +
  provenance + hash switcher + copy-citation on all pages. Baseline v1
  prototype and `app/` untouched. Sibling SEO/SSR pass = H227 (separate).

## ✅ Completed (Recent only) — 2026-07-04
- **`app/` slice 1 SHIPPED** (Fable 5 `claude-fable-5`, H147, spec
  [ui-spec-app-v1.md](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/doc/ux-redesign/ui-spec-app-v1.md)):
  `app/index.php` (lookup-pattern GET prefill: key/input/output/dict/mode),
  `app/app.js`, `app/app.css` (prototype palette), `app/vendor/sanskrit-util.global.js`
  (byte-identical vendored copy of the sanskrit-util JS global build — no new
  transcoder), `app/fixtures/` (`?fixtures=1` offline mode; real 2026-06-11
  fuzzy lists, synthetic watermarked dalglob/entry payloads,
  [readme](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/app/fixtures/readme.md)).
  Fuzzy/Exact/Prefix full, Suffix tab disabled ("slice 2"); results grouped by
  headword with dictmeta-ordered dict badges + MW¹/MW² homonym sub-badges;
  entry reader with servepdf scan link + permalink copy; IAST⇄Devanagari
  toggle; auto-detect badge; mobile accordion. Rate discipline honored:
  single-flight token, 300ms debounce, session cache (verified 0 fetches on
  repeat search), no prefetch, 250ms-spaced sequential badge chain, batch-404
  → sequential getword.php + 429 backoff (lookup.js fallback). All 8
  acceptance criteria verified offline in a real browser (XAMPP `php -S` +
  preview harness) at 1280px and 375px. Two bugs found+fixed during
  verification: `.ap-advanced` display:grid defeating `[hidden]`; MQL
  viewport listener made GC-safe + resize fallback. Live checks and fixture
  recapture remain gated on the server outage (see Next Steps).

## ✅ Completed (Recent only) — 2026-07-03
- **Cologne UX redesign prototype drafted** (Codex GPT-5): added
  `doc/ux-redesign/cologne-redesign-prototype.html` and
  `doc/ux-redesign/proposal-brief.md`. Direction follows user decisions:
  Simple Search becomes the main experience; Basic/List/Advanced/Mobile collapse
  into one responsive interface; homepage prioritizes fast search + dictionary
  browsing; scans remain linked actions; exact/prefix/suffix search and PHP URL
  compatibility are must-preserve. Prior-art verdict PARTIAL: reuse
  `csl-websanlexicon` endpoint/templates, `simple-search` roadmap, and
  `lookup/` UI precedent; build only the UX proposal/prototype gap. Verified
  locally in Chrome via Playwright: proposal switching and mobile frame render.
- **`lookup/` Wave 2 shipped** (Sonnet 5 `claude-sonnet-5`, H095): dictionary
  result cards (full title/edition-year from new `lookup/dictmeta.js`, a
  static table merging `sample/dictnames.js` + `dictinfo.php`'s
  `DictInfo::$dictyear`/`$dictyear_older` — no new PHP endpoint, per the
  roadmap's lighter option); language filter chips (title-derived
  en/de/fr/la/other, mechanical classification, not a scholarly claim) +
  edition-year filter chips; per-user preferred-dictionary pin persisted in
  `localStorage`, sorted first. `?key=&input=&output=` permalinks via
  `history.pushState` on every search + a `popstate` handler so back/forward
  re-runs the search from the URL (verified live in a fixture-stubbed
  browser harness: pin/filter/permalink/back-button/copy-link all correct).
  Copy-link button (Clipboard API + textarea/execCommand fallback). Wave 1
  endpoint contracts untouched (`dalglob.php`/`getword.php`/
  `getword_batch.php` diff = empty); `index.php` diff is additive only.
  [PR #67](https://github.com/sanskrit-lexicon/csl-apidev/pull/67), auto-merge armed.
- **H122 review fix-ups applied** (Sonnet 5 `claude-sonnet-5`,
  [PR #66](https://github.com/sanskrit-lexicon/csl-apidev/pull/66) merged): all
  1 CRITICAL + 3 MAJOR + 7 MINOR + addendum A1/A2 + NITs from the Fable 5
  review resolved. C1: deleted Fix C rule (b) (vetoed guṇa/maṇi-class ṇ in all
  modes). M1: added `expect`/`aspirational` column to `gold.tsv` +
  `eval_search.py` support so the recall@5 gate excludes Stream-A rows instead
  of being unsatisfiable by construction; added `mani`/`guna`/`hari`/`giri`
  C1/M3 tripwire rows (fixtures pending live capture). M2 (MG ruling via
  AskUserQuestion: re-mark the gold row): `rama` now `aspirational` —
  recall-only, not a P@1 target; withdrew the "rama P@1→1.0" promise from 3
  docs. M3: repaired Fix F folknorm (ksh-before-sh reorder, `ri→f` onset-
  anchored, dropped the geminate-destroying doubling-collapse). MINORs:
  ops-cheatsheet master→main; line-vs-key wording; Fix E1 NFC example swap;
  Vidyut MIT; DH-roadmap baseline superseded label; repaired 6 mojibake DCS
  lemmas (U+FFB1/U+FFDE) in `build_wf_from_dcs.py` + regenerated `wf1/wf.txt`
  and `dcs_cdsl_xref.tsv` (filed [VisualDCS#10](https://github.com/gasyoun/VisualDCS/issues/10)
  upstream); `min()` empty-guard + h/H transitionCost row note; addendum A1
  legacy-spelling second-normalize pass recovers 89 dead pre-2017 keys / 396
  DCS tokens (verified exact match to the addendum's own figures); `in_cdsl`
  re-documented as `in_wf0` semantics. Re-synced issue #47 from the updated
  file. Only [H122](https://github.com/gasyoun/Uprava/blob/main/handoffs/H122-Sonnet_csl-apidev_simple_search_review_fixups_03.07.26.md)
  step 8 (live re-run, blocked on the Cologne outage) remains — see Next Steps.
- **Fable-5 adversarial review of the simple-search overhaul EXECUTED** (Fable 5
  `claude-fable-5`, per [simple-search/review_handoff_fable5.md](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/simple-search/review_handoff_fable5.md)):
  full report in [simple-search/review_fable5_findings.md](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/simple-search/review_fable5_findings.md).
  Re-derived, not trusted: dalnorm + transcoder ports diffed rule-by-rule vs frozen PHP
  (**faithful**); `wf1/wf.txt` + `dcs_cdsl_xref.tsv` re-run → **byte-identical** (sha256);
  eval baseline recomputed by hand (all 5 numbers exact); 94.7% token-mass + 134,047
  total verified; all 36 gold `intended` = real MW headwords landing on wf0 keys;
  csl-standards scoping claims all real. VERDICT: (a) wf1 deploy-safe YES;
  (b) Fixes A–I NOT as written — Fix C rule (b) CRITICAL (vetoes guṇa/maṇi-class ṇ-words
  in all modes), Fix F 3 defects, eval gate unsatisfiable live (M1) + impossible
  rama-P@1 promise (M2); (c) DH plan sound, scoped correctly (Vidyut is MIT not
  Apache-2). Live API re-hit BLOCKED: Cologne host dark mid-review from two egresses
  (429 burst → TLS kills; vedaweb already on the outage board) — counts rest on the
  2026-06-11 fixtures; rerun `eval_search.py --live` gently when host returns.
- **H118 differential-execution addendum** (second Fable 5 `claude-fable-5` pass, same
  day, concurrent-session reconciliation): the frozen PHP was **executed** (XAMPP PHP
  8.2.12 CLI) against the ports — `normalize()` 0 diffs on 50,574 wf0 keys + 14,989
  real SLP1 inputs (only unreachable non-ASCII PCRE-byte edges differ); the real
  `utilities/transcoder.php` 0 diffs on all 15,902 lemmas + adversarial IAST; NFC
  proven strictly protective (recovers all 11,068 NFD-mangled). New: A1 458 dead wf0
  keys (89 refreshable normkeys / 396 DCS tokens forfeited — `pracC`←`praC` 91 …),
  A2 `in_cdsl` overclaim (wf0 ≠ CDSL head-key universe), A3 f-doubling leak 28
  keys / 205 tokens pending one `vftti` live probe, A5 preverbs verified non-issue,
  A6 precise-bucket ceiling correction 7/9 ≈ 0.78. C1/M1/M2/M3 all independently
  CONFIRMED; verdicts CONCUR. Addendum appended to
  [simple-search/review_fable5_findings.md](https://github.com/sanskrit-lexicon/csl-apidev/blob/main/simple-search/review_fable5_findings.md);
  A1–A3 folded into [H122](https://github.com/gasyoun/Uprava/blob/main/handoffs/H122-Sonnet_csl-apidev_simple_search_review_fixups_03.07.26.md)
  steps 5/8.
- **H122 review fix-ups landed** (Sonnet 5 `claude-sonnet-5`, PR
  [#66](https://github.com/sanskrit-lexicon/csl-apidev/pull/66), merged): all
  1 CRITICAL + 3 MAJOR + 7 MINOR + addendum A1/A2/A3 findings applied.
  C1 — Fix C rule (b) deleted from `roadmap_v1.2.md` §6 (kept rule (a) +
  ṅa/ṇa Q8 whitelist). M1 — `eval/gold.tsv` gained an `expect`/`aspirational`
  column; `eval_search.py` excludes those rows from the ALL/default/precise
  gate and reports them separately; added `mani`/`guna`/`hari`/`giri`
  tripwire rows. M2 — `rama` marked `aspirational` (MG ruling: recall-only,
  not P@1 — `put_user_word_first` is a kept design); the "rama P@1→1.0"
  promise withdrawn from all three docs. M3 — Fix F `folknorm` reordered
  (`ksh` before `sh`), `ri→f` anchored to `/^ri/`, blanket geminate-collapse
  dropped, `user_keyin`-before-`folknorm` note added. MINORs — ops-cheatsheet
  master→main sweep; line-vs-key wording fixed; Fix E1 example swapped to a
  genuinely NFC-composable case; Vidyut MIT (not Apache-2); DH-roadmap
  baseline labeled superseded; 6 mojibake DCS lemmas (U+FFB1/U+FFDE) repaired
  in `build_wf_from_dcs.py`, `wf1/wf.txt` + `dcs_xref/dcs_cdsl_xref.tsv`
  regenerated (**re-verified byte-reproducible this session** — sha match on
  both artifacts); upstream defect filed as
  [VisualDCS#10](https://github.com/gasyoun/VisualDCS/issues/10); `min()`
  guarded on empty `searchcost`; h/H transitionCost row noted. Addendum A1 —
  DCS normkeys now also matched against a second `dalnorm_normalize()` pass,
  recovering 90 legacy pre-2017 spellings (`pracC`←`praC`, `icCA`←`iCA`, …).
  Addendum A2 — `dcs_xref/readme.md` re-documents `in_cdsl` as `in_wf0`
  semantics (wf0 ≠ CDSL head-key universe). Issue #47 re-synced from the
  updated `issue_jim_implementation.md`. **Jim unblocked for M3** (Fixes
  C/F) — the wf1 one-line switch (Fix I) was already GO and untouched by
  this pass. Live rerun (`eval_search.py --live` + the A3 `vftti`/`praC`/`ca`
  probes) still **blocked** — Cologne host unreachable this session too (see
  Next Steps).
- **`lookup/` Wave 1 shipped** (Sonnet 5 `claude-sonnet-5`, H088): new
  `lookup/index.php`+`lookup.css`+`lookup.js` (vanilla ES6, no CDN/framework,
  accessible ARIA homonym tabs, native `<datalist>` autocomplete, Devanagari/
  IAST auto-detect + explicit HK/SLP1/ITRANS select) + additive
  `getword_batch.php` (one round-trip per dictionary, sequential+backoff
  fallback). Existing `dalglob.php`/`getword.php`/`getsuggest.php` untouched
  (verified empty diff). Verified live against the Cologne API (egress worked
  from this host today) — `dalglob.php` for "agni" returns 32 dictionaries in
  the expected shape; `getword_batch.php` correctly 404s (not yet deployed),
  exercising the fallback path as designed. Found + worked around a
  pre-existing dead-code path in `getwordClass.php` (`dispcss=no` never
  actually suppresses the entry `<link>` — `basicOption` always overwrites it)
  via the same client-side CSS-path rewrite `sample/dalglob1.php` already
  uses. [PR #63](https://github.com/sanskrit-lexicon/csl-apidev/pull/63),
  heads-up posted on
  [#16](https://github.com/sanskrit-lexicon/csl-apidev/issues/16).

## ✅ Completed (Recent only) — 2026-06-11
- Reverse-engineered the v1.1 engine from source + pulled real result counts from
  the live MW endpoint (0/1/5/11/18/20 buckets).
- Enriched `simple-search/readme.org`: what-it-is, human + API URLs, #26 anchor,
  mermaid data-flow, stage-by-stage, equivalence-table explainer, four worked
  result examples (0/1/5/15+) with real JSON, overgeneration analysis, input-
  coverage gaps (incl. capital letters), pointer to the roadmap.
- Wrote `simple-search/roadmap_v1.2.md` in Jim's idiom (numbered §§, live example
  URLs, PHP rewrite blocks, Expected-output JSON, target-pipeline mermaid, M1–M5
  sequencing, 10 Questions). Covers Fixes A–H: tiered tables, score+hard-drop,
  phonotactic prune, dedup, NFC+detect, folknorm, new tables, index-side rewrite.
- Confirmed #26 = Jim's v1.1 rollout/feedback issue (open, enhancement/minor,
  User Experience); gives canonical short URLs /simple/ (prod) + /simplet/ (test).
- Committed + pushed **59b70a3** `docs(simple-search): v1.2 improvement roadmap +
  enriched readme` (readme.org + roadmap_v1.2.md + this journal) to master.
- Pulled #26 thread: **zero comments** (just Jim's /simplet/ announcement) — the
  drafted feedback comment won't duplicate anything. Could not post it: this host
  cannot reach api.github.com (gh times out; github.com git push works).
  User confirmed they posted the comment to #26 manually.
- Added roadmap **§12 Fix I** (refresh ranking frequencies from DCS-2026) + a
  measured coverage probe; renumbered §§13–15; added Q11–Q13 (freq source, merge
  policy, freq×edit-score).
- Wrote **`simple-search/roadmap_dh.md`** — DH-grade program (4 streams) in Jim's
  idiom, aligned to csl-standards; consolidated Questions.
- **BUILT Stream D eval harness** (`simple-search/eval/`): `eval_search.py` (P@1,
  recall@K, MRR, mean#results), `gold.tsv` (16 seed rows, query→intended), cached
  `fixtures.json`, `readme.md`. v1.1 baseline (wf0): recall@5=1.00, **default
  mean#results=6.67 vs precise 1.00** (overgeneration quantified); P@1=0.94 (one
  miss: rama→rAma@2). This is the regression gate for Fixes A–I.
- **DCS↔CDSL crosswalk** built+pushed (`simple-search/dcs_xref/`, f70668a + a155471
  CHANGELOG): 15,902 lemmas→CDSL normkey, 12,946 (81.4%) linked, 2,956 DCS-only.
- **Eval gold set expanded 16→43** (fc4c5c9): +folk romanization, common headwords,
  precise IAST, Stream-A lemmatization targets, extra zero-result; +6 fixtures;
  harness now skips un-fixtured rows offline. New baseline: default mean#results
  6.67→5.22 (22 fixtured rows), recall@5=1.00, P@1=0.94 (rama still the one miss).
- **Repo-boundary decision locked** (roadmap_dh §0.5 + §8 Q5): csl-standards owns
  model+TEI/OntoLex+loss; simple-search owns retrieve+rank+address+corpus-ground.
  Open sub-question: who owns the DCS↔CDSL crosswalk.
- **Master Jim handoff** (`simple-search/issue_jim_implementation.md`) → opened as
  **csl-apidev#47** (labels enhancement/major, milestone User Experience, assigned
  funderburkjim): ordered checklist Phases 0–5 + DH Streams A–D + locked decisions
  + all open questions.
- **BUILT Fix I** (`simple-search/wf1/`): `build_wf_from_dcs.py` (Py3, faithful —
  parses the repo's roman_slp1.xml for IAST→SLP1, ports dalnorm.normalize() regex
  exactly; PHP + hwnorm1c.sqlite are NOT on this host, so no DB needed), generated
  `wf1/wf.txt` (drop-in for wf0, 50,574 keys), `readme.txt`. 12,096 keys refreshed,
  1,573 zero→positive. Spot-checks: tad 180→3734, ca 179→3385, kf 163→1083,
  rAjan 84→588, agni 124→295. No BOM, LF. Marked built in roadmap §12.
- **Reconciled `doc/cleanurl.md` with the Salt API permalink** (`salt_entries.md`):
  both define the same `/{DICT}/{ref}` URL, so unified them as one content-negotiated
  rewrite — cleanurl = HTML/listview + collision-safe-routing face; Salt = JSON face
  (`Accept: text/html` → `cleanurl.php` listview, `application/json`/`?format=json` →
  `salt_entries.php`). Carried back the two things Salt's §1.7 draft dropped: the
  dict-code **whitelist** (its `^([A-Za-z0-9]+)/([^/]+)$` captured every 2-segment root
  path, e.g. `/images/x`) and **homonym/decimal-lnum** handling. Edits: cleanurl new
  §0 + Q8 (content-negotiation trigger), salt_entries §1.7 reconciliation note,
  readme.md row 'subsumed'→'unified/HTML face'. Committed + pushed to master.
- (Earlier this session, clobbered from this journal by the v1.2 overwrite — restored
  for the record) **README manual-block guard**: wrapped the cleanurl Documentation
  block in `<!-- BEGIN/END MANUAL -->` markers; hardened both runbook skills to
  preserve marked blocks verbatim (+ match `doc/` not just `docs/`); added CI guard
  `.github/workflows/readme-guard.yml` — strict in csl-apidev, rollout-safe diff
  variant merged to **mw-dev#24, csl-app#41, csl-doc#7, sanskrit-lexicon.github.io#1**
  (csl-websanlexicon + csl-homepage intentionally excluded).

---

# Parallel thread — Salt API Phase 1 (PR #46, branch `salt-api-phase1`)

C-SALT-compatible API (`/dicts/{id}/restful/entries|ids`, `/graphql`) over the existing
`getword` data — three `api1/` controllers sharing `salt_common.php`. MW pilot. Contract +
roadmap in `csl-standards` (`SALT_API_*`). This thread is **run-verified locally** this
session; remaining work is the C-SALT parity pass (Jim) + Phase 5.

## ➡️ Next Steps (Salt API)
- **Jim:** run `python data/pilot/parity_mw.py` (csl-standards) against `api.c-salt.uni-koeln.de`
  to settle: the `-L{lnum}` id fallback vs C-SALT's `-{n}`; the `prefix` `size` unit
  (records vs headwords); homonym ordering. Then flip PR #46 draft → ready.
- **Phase 5:** populate `sense[]`, `re_headwords_slp1[]`, and TEI `xml` (from CDSL→TEI);
  apply `transcoder_processElements` so `csl.html` is display-script, not SLP1-tagged.
- **Phase 4:** body search — `regexp`/`match`/`match_phrase` (now 400) via SQLite FTS5 / ES.
- **Deploy:** add the Apache rewrites (handoff §3) on the Cologne host; reconcile the
  `/{DICT}/{ref}` permalink with `cleanurl.php` (content-negotiated, dict-code whitelist).

## 🚧 Current Work-In-Progress (WIP)
- None open for Salt API in this repo. Phase 1 field-handling follow-up is on PR #59;
  deployment handoff is documented in `doc/salt_api_handoff.md`.

## 🧠 Dev Notes & Hypotheses (Salt API)
- **Local run recipe (reproducible):** PHP 8.2 + pdo_sqlite present (`/c/xampp/php/php.exe`).
  Per-dict data = `csl-sqlite` GitHub Releases `{dict}.zip` (mw.zip ≈26MB → mw.sqlite, NOT
  `download_hwnorm1c_sqlite.sh` which only fetches the suggest helper). Stage at
  `../{dict}/web/sqlite/`; `dictinfo::get_webPath` (xampp) needs the repo dir basename to be
  literally `csl-apidev`. Then `php api1/salt_selftest.php mw agni indra ka`.
- **Render chain:** `Getword_data(false)` → `get1_mwalt` → `BasicAdjust` → `BasicDisplay`
  (which `exit(1)`s on a malformed info string) → MW info `page,col:hcode:key2:hom:hui`. A
  hand-built fixture is too fragile; use real csl-sqlite data.
- **3 run findings:** (1) id non-uniqueness — **FIXED** (`-L{lnum}` fallback, commit 90e58f1);
  (2) `prefix` returns records-not-headwords — parity Q; (3) `csl.html` SLP1-tagged — Phase 5.
- **Security:** JSONP-callback reflected-XSS fixed in api1/ (#46) + 10 master endpoints (#52,
  merged). Semgrep `echoed-request` is taint-mode → must `htmlentities`/whitelist the echoed
  callback, not just guard it.

## ✅ Completed (Salt API) — 2026-06-14
- 2026-06-20 **Phase 1 docs alignment**: endpoint specs, use cases, README pointers, and
  GraphQL examples now match the deploy handoff (`api1/` paths, explicit Phase 1 caveats,
  variable-based GraphQL `ids`, and migration wording).
- 2026-06-20 follow-up: REST/GraphQL now support only `field=headword_slp1` in Phase 1;
  other valid C-SALT field enum values return an explicit unavailable-field error until
  Phase 4/5 resolvers or indexes exist. Also guarded shared Salt search so a checkout
  without `mw.sqlite` returns empty envelopes instead of fataling on a null PDO handle.
  Verified with `C:\xampp\php\php.exe`: PHP lint, no-sqlite selftest, REST `field=sense`
  error, and GraphQL `field=sense` error.
- Hardened JSONP-XSS in `salt_entries.php`/`salt_ids.php`; aligned to the `400-invalid-callback`
  idiom; CI (Semgrep OSS) green. Swept the same class across 10 `master` endpoints → PR #52 (merged).
- Downloaded real MW data and **run-verified all three faces end-to-end** (exit 0, correct envelopes).
- **Fixed entry `id` uniqueness** (`-L{lnum}` fallback) — `ids` now resolves a single record; re-verified.
- Updated `doc/salt_api_handoff.md` (status → run-verified, §2.1 data recipe, §4 punch-list);
  deepened `doc/salt_entries.md`/`salt_ids.md`/`salt_graphql.md` with real responses + `query_type`
  matrix + error/JSONP semantics; added `doc/salt_api_usecases.md` (10 recipes); CHANGELOG entries.
- Merged `origin/master` into the branch (picks up CHANGELOG.md + the #52 security sweep).
